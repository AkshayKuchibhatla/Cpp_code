#include <iostream>
using namespace std;

int main() {
    /*when a program is run with an open expression,
    the compiler generates a warning and it doesn't 
    return anything when it's run.*/
    3 + 7;
    return 0;
}